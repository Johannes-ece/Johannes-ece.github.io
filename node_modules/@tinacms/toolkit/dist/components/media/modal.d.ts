/// <reference types="react" />
interface DeleteModalProps {
    close(): void;
    deleteFunc(): void;
    filename: string;
}
export declare const DeleteModal: ({ close, deleteFunc, filename, }: DeleteModalProps) => JSX.Element;
export declare const SyncModal: ({ close, syncFunc, folder, branch }: {
    close: any;
    syncFunc: any;
    folder: any;
    branch: any;
}) => JSX.Element;
export {};
